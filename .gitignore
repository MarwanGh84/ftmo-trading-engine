# secrets — never commit
.env
*.key
*.pem
secrets/

# runtime state (changes every run — not useful in history)
state.json
.state.lock
logs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
dist/
build/

# macOS
.DS_Store

# reports (regenerate from code)
SYSTEM_REPORT.docx
